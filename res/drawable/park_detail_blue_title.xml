<?xml version="1.0" encoding="UTF-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >

    <!-- 填充的颜色 -->
    <solid android:color="@android:color/white" />

    <stroke
        android:width="@dimen/dimen_line_shape"
        android:color="@color/blue_detail" />
    <!-- 设置按钮的左上角和右上角为圆形边框 -->
    <corners
        android:bottomLeftRadius="@dimen/dimen_corner"
        android:bottomRightRadius="@dimen/dimen_corner" />

</shape>